name: dispatch-target
on:
  workflow_dispatch:
    inputs:
      payload:
        description: "payload"
        required: true
jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write
    steps:
      - run: echo "dispatch recebido: ${{ inputs.payload }}"
